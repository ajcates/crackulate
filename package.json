{
  "name": "crackulate",
  "version": "1.0.0",
  "description": "a mobile crackulator",
  "main": "index.js",
  "scripts": {
    "dev": "live-server --port=8080 --host=localhost --open=/",
    "test": "live-server --port=8080 --host=localhost --no-browser"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "git-scripts": "^0.6.4",
    "live-server": "^1.2.0",
    "surge": "^0.0.0"
  },
  "git": {
    "scripts": {
      "pre-push": "surge --project ./ --domain crackulate.surge.sh"
    }
  }
}
